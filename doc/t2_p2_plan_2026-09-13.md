# Task 2 · 2026-09-13 冲榜

- 账本：[t2_p2_val_2026-09-05.md](t2_p2_val_2026-09-05.md)（§15–§16）
- 执行日：09-13；每日 8 格；三榜分开、榜留最好
- 现役：心脏插值 **68.94** / 外推 **54.66** / 胚胎 **67.11**（均 **63.57**）
- 第十：**65.0 / 56.8 / 73.8**（均 **65.20**）；第一：**73.4 / 59.8 / 73.6**

**结论先说：** 进前十只可能靠胚胎。空间分层 slice 已在 09-12 下午过留出门（账本 §17）：**ODS 修好了，AND 门因 mmd 税 16% 未过，不交。** 明天不要重复这条。过不了新的 X 门就空过。心脏 / 外推 0 格。

---

## 1. 分差（不要再算错）

| 榜 | 我们 | 第十 | Δ | 明天 |
|----|------|------|---|------|
| 心脏插值 | **68.94** | 65.0 | **+3.94** | 锁 04。n=17616 / 再排行 / slice / crop / span-trim **禁** |
| 心脏外推 | 54.66 | 56.8 | −2.14 | 锁 G0。Δ / 加 n / 组成 / RNA **禁** |
| 胚胎插值 | 67.11 | **73.8** | **−6.69** | **唯一通道** |

67.11 题组：expr 64.7 / state 65.8 / shape 72.7 / NFS 65.2。shape 已经不像洞；de 59.5、NFS 65.2、mmd 66.4 才是。第十与第一胚胎几乎同分 ⇒ 缺的是方法，不是格子。

09-12 已兑现、不要重复：RMS 198.24、slice band 0.25 的 X、冻到 H 坐标、span-trim auto、心脏 A 序列。01 自带几何 **禁交**。

---

## 2. 09-12 留下的机制事实

1. **slice 已经在表达轴上。** `_progress_axis` 吃的是 `encode(X)`（panel / PCA），不是 xyz。keep=0.25 是代理峰。再扫 keep / tail **禁**。
2. **中期细胞在左胚上占不住全图。** 01 = 同一套左锚 TPS 场上的表达-band 子集，nocc 195≈目标 188，ODS 却 42.3。是**错体素**，不是数错。全局表达切片与解剖位置相关，细胞挤进子域。
3. **冻到全跨度几何能救 ODS、但拆配对。** 02/03：ODS 59.8→63.9，NFS 68.8→65.2。Hungarian 把中期 X 贴到全胚坐标，这就是 73.8 队大概没付的税。
4. **panel OT-CFM 生成式 X 已关**（variogram 4–6×）。明天仍是真细胞，不训练 flow。

推论：73.8 不是「再切一刀表达」，是 **在每个空间邻域里切表达**，让占据图仍像 H、X 仍像 01。

---

## 3. 主线：空间分层 slice（`--ot-x slice --slice-stratify spatial`）

按簇、在左锚规范坐标上分箱（先 8³；16³ 若每箱太稀再退回 8³）。箱内对表达进度做与现在相同的 band keep=0.25；**按箱的占据比例抽细胞**，不许空掉外周箱。xyz 默认 `left`（细胞留在本箱的 TPS 左坐标）。右锚细胞只从 OT 配到同一箱的伙伴里抽。

期望（相对 mix / 相对 01）：

| | mix（现役类） | 01 全局 slice | 分层 slice |
|--|---------------|---------------|------------|
| X | 全阶段混合 | 中期 | 中期 |
| 占据 | 全图 | 子域（ODS 42） | 近全图 |
| NFS | 原生、阶段错 | 原生、阶段对 | 原生、阶段对 |

### 3.1 留出门（不过门不交）

窗口仍是 E6.75+E8.0 → 真 E7.25（`27_slice_gate.py`）。**今天的门缺 ODS，01 才上了线。** 明天门必须带形状：

对照：`gate725/mix.h5ad`（OT-place pick + TPS + xyz left）。

| 条件 | 过门 |
|------|------|
| de | ≥ mix − 0.02，且不差于全局 slice 太多 |
| NFS | **优于 mix**（这是分层相对 01 冻坐标的意义） |
| mmd | 不差于 mix 的 14% 以上；能优于 mix 更好 |
| ODS vs 真 E7.25 | **不低于 mix − 0.02 raw**（禁止再交 01 那种占位崩） |
| d2 | 不比 mix 差一个心脏 span-trim 那种量级 |

AND 门。NFS 赢但 ODS 崩 → 不交原生几何；只允许把该 X 冻到现役 03 坐标（门槛 > 67.11，盯 NFS 是否仍优于 65.2）。ODS 赢但 de/NFS 不赢 → 关分层，空过。

### 3.2 交榜顺序（最多 3 格，其余空）

过门才生成文件。门槛一律 **> 67.11**。RMS **198.24**。n=5000 + `--ot-unique`。

1. **原生几何**（分层 slice + xyz left + 现役 TPS 场）。这是冲 73.8 的那一格：保 01 的 NFS、保 03 的 ODS。
2. **冻到 03 坐标**（`19_coord_swap` + `26` 行序）。ODS/d2/SLR 锁 63.9/54.3/100；只赌新 X。第 1 格 ODS 不够时才交。
3. **第 1 格 + span-trim auto**（仅当原生 nocc 明显高于 188，且第 1 格已过门）。union/intersect 仍禁。

不要交：keep 网格、tail、xyz=own（两期叠云）、xyz=lerp、occ、心脏分层、09-11 旧胚胎文件。

---

## 4. 明确不做什么

- 心脏插值、心脏外推：**0 格**。
- 再训练 OT-CFM / `--use-flow`。
- 表达 Δ、pb 重定向、RNA、E6.75 三次轨迹估 pb。
- 离线搜行序、去噪序叠在 68.94 或 67.11 上。
- 为「格子别浪费」而交未过门文件。

---

## 5. 时间盒

| 时段 | 事 |
|------|----|
| 早 | 实现 `--slice-stratify spatial`；扩展 `27_slice_gate.py` 打 ODS/d2 |
| 午前 | 留出窗：mix vs 全局 slice vs 分层；写进账本 §17 |
| 过门 | 造第 1 格，交；出分后再决定格 2/3 |
| 不过门 | 停。余格空过。不要用线上试 keep |

复现：

```bash
.venv/bin/python scripts/13_predict_t2.py --setting embryo --target 7.25 --method full \
  --shape tps --ot-interp --ot-x slice --slice-mode band --slice-keep 0.25 \
  --slice-stratify spatial --ot-xyz left --n 5000 --ot-n-pair 6000 --ot-unique \
  --flow-dir outputs/t2/embryo/gate725 \
  --out outputs/t2/embryo/gate725/slice_strat_spatial.h5ad
.venv/bin/python scripts/diagnostics/27_slice_gate.py --window embryo \
  --mix outputs/t2/embryo/gate725/mix.h5ad \
  --cand band=outputs/t2/embryo/gate725/slice_band0.25.h5ad \
  --cand strat=outputs/t2/embryo/gate725/slice_strat_spatial.h5ad
```
